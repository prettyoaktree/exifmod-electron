/** Stable code returned by main `ollamaDescribeImage` when JSON parses but description and keywords are both empty (renderer maps to i18n). */
export const OLLAMA_ERROR_EMPTY_SOFT = 'OLLAMA_EMPTY_RESULT'

/** Model returned text that matches a known default-prompt example / placeholder instead of describing the image. */
export const OLLAMA_ERROR_ECHO_TEMPLATE = 'OLLAMA_ECHO_TEMPLATE'
