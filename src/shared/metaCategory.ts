/**
 * Category keys for file-list pending icon chips and metadata row icons (Approach D).
 * Shared so renderer and helpers stay aligned without circular imports.
 */
export type MetaCategory =
  | 'camera'
  | 'lens'
  | 'film'
  | 'author'
  | 'shutter'
  | 'aperture'
  | 'desc'
  | 'keywords'
